
prep = lathe.Workflow("prep")

prep.Add(lathe.Process({
  commandLine: "../gdc/gdc-scan.py cases {{outputs.cases}}",
  outputs:{
    cases: "../../source/gdc/cases.json"
  }
}))
  
prep.Add(lathe.Process({
  commandLine: "../gdc/gdc-scan.py files {{outputs.files}}",
  outputs:{
    files: "../../source/gdc/files.json"
  }
}))

