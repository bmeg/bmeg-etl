

prep = lathe.Workflow("prep")


prep.Add(lathe.Process({ 
    commandLine: 'curl -X POST https://pharmacodb.ca/graphql -H "Content-Type: application/json" -d \'{"query":"{ compounds(all: true) { id name annotation { smiles inchikey pubchem fda_status } } }"}\' -o {{outputs.compounds}}',
    outputs: {
       compounds: "../../tables/compounds_table.json"
    }
}))